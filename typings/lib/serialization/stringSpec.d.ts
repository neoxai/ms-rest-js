import { TypeSpec } from "./typeSpec";
/**
 * A type specification that describes how to validate and serialize a string.
 */
declare const stringSpec: TypeSpec<string, string>;
export default stringSpec;
